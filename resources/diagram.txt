# sequencediagram.org

participantgroup #white #lightgray:1 **Host Machine**

participant "Host Container API" as host

participantgroup #lightgreen :0 **Shared volume**
participant "IN pipe" as in
participant "OUT pipe" as out
end

participantgroup #lightblue :0 **Docker**
participant "Container" as container
end

end

host->in: read content
activate in
host->host: wait
container->in: write string
in-->>host: send string
deactivate in
destroysilent in

container->out: read content
activate out
container->container: wait
note over host: run command(s)
host->out: write results
out-->>container: send results
deactivate out
destroysilent out
